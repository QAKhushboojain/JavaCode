package Operators;

public class Day_46_Notes {
    // Quick checklist (summary for testers)
    //i++ returns old value and then increments.
    //++i increments first then returns new value.
    //Java evaluates operands left to right.
    //Compound assignment (+=) performs implicit cast if needed.
    //++/-- not allowed on boolean.
    //Avoid expressions with multiple side-effects on same variable — they’re unreadable and error-prone.
    //byte/short promotion: x += y vs x = x + y.
    //Parameter evaluation in method calls is left→right.
}
